export const sharedFunction = () => {
    console.log("This is a shared function.");
};

export const sharedConstant = "This is a shared constant.";

export interface SharedInterface {
    id: number;
    name: string;
}